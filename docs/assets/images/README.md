# Generated images

This directory is reserved for images used by the GitHub Pages website.

The `locus` and `vis-spec` programs write `out.bmp`. Rename a generated image to a descriptive filename before placing it here so that subsequent runs do not overwrite it.
